# Lightweight CI dependencies for scrape + aggregate + test pipeline
# Avoids heavy system-dependent packages (spacy, opencv, geopandas, tesseract, etc.)

# Core data
pandas>=1.5.0
numpy>=1.21.0
pydantic>=2.0.0
openpyxl>=3.0.0

# Scraping
requests>=2.28.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
pyyaml>=6.0.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
